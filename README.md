# odoo-infra-scripts

Reusable infrastructure scripts for Odoo Community servers.

## Modules
- nginx: reverse proxy templates + scripts (IP-only first, DNS/SSL later)

## Workflow
1) Clone repo on server
2) Copy env/example.env -> env/.env
3) Run scripts in order